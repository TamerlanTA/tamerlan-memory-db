# Michael Dang

## Related
- [[agent-memory]]
- [[current-focus]]
- [[routing-rules]]

## Summary
Newly started project workspace at `/Users/tamerlan/Desktop/michaeldang`.

## Goal
Define the product clearly, choose the implementation stack, and turn the empty workspace into a working first version.

## Current Status
- 2026-05-18: Created the initial repository skeleton in `/Users/tamerlan/Desktop/michaeldang`.
- 2026-05-18: Defined the actual MVP as a lean n8n AI content workflow for Michael Dang using Google Sheets, OpenAI, and Buffer.
- Added two importable workflow drafts, review-table schema, sample calendar CSV, architecture notes, testing plan, and client handover docs.
- 2026-05-18: Performed local workflow inspection and smoke-test pass; hardened error handling and Buffer fallback behavior, added setup checklist and smoke-test report.
- 2026-05-18: Prepared last-mile implementation package: credential map, live test plan, client access request, and final delivery status.
- 2026-05-21: Updated project for Bastion & Mews real client materials: final CSV/XLSX content calendar template with 6 starter rows, brand-specific content prompt, official n8n OpenAI node in Workflow 1, LinkedIn optional/pending behavior in Workflow 2, and revised setup/test/handover docs.
- 2026-05-21: Added image-generation MVP path: expanded schema to 35 columns, Workflow 1 now outputs `image_prompt` and moves rows to `Needs Image`, new Workflow 3 generates one OpenAI image per row and stores it in Google Drive, Workflow 2 attaches `image_url` to Buffer posts where supported.
- 2026-05-21: Hardened Google Drive image URL -> Buffer asset flow: Workflow 3 now writes direct Drive download URLs, docs require incognito/public fetch test, Workflow 2 writes `buffer_instagram_status = Missing Image` when Instagram lacks `image_url`, and Buffer/media failures route to manual scheduling.
- 2026-05-21: Final live acceptance prep completed. Could not import/run workflows because local n8n API requires `X-N8N-API-KEY` and no service credentials were present. Updated smoke report, final delivery status, and handover to state blocked/not handover-ready until positive + negative live tests pass.
- 2026-05-21: Added `docs/operator-live-setup-runbook.md`, a step-by-step manual n8n setup/testing checklist covering imports, credentials, placeholders, expected red warnings, test row, run order, screenshots, and Michael delivery/support message templates.
- 2026-05-23: Added Google Apps Script at `/Users/tamerlan/Desktop/michaeldang/scripts/create-google-sheets-template.gs` to create the `content_calendar`, `settings`, and `live_test_checklist` tabs with headers, starter rows, validation dropdowns, formatting, and QA checklist.
- 2026-05-23: Fixed Workflow 1 parser after live n8n run showed OpenAI producing 6 items but parse/update only handling 1 item. `Parse and Validate JSON` now loops over all OpenAI outputs and pairs each with the matching `Filter Ready to Generate` row; OpenAI system prompt also explicitly includes `image_prompt`.
- 2026-05-23: Live n8n OpenAI node showed rate-limit error (`The service is receiving too many requests from you`) because all 6 starter rows were `Ready to Generate`. Updated CSV/XLSX/App Script so only `bm-001` starts as `Ready to Generate`; rows `bm-002`..`bm-006` start as `Draft`. Runbook now warns to test one row at a time.
- 2026-05-23: Rechecked exported live Workflow 1 (`workflows/Michael Dang - 01 Content Generation.json`) after rate-limit repeated. Found live export still had old `Filter Ready to Generate` that passed all Ready rows and old single-item parser. Patched both exported-name and canonical workflow JSON: filter now hard-limits to `MAX_ITEMS_PER_RUN = 1`, trims status, and parser handles all OpenAI outputs robustly. User still must paste/update these nodes in n8n; local JSON changes do not affect already-imported workflow automatically.
- 2026-05-23: OpenAI now succeeds and returns nested `output[0].content[0].text` containing fenced JSON. Patched `Parse and Validate JSON` locally to extract that nested path and strip ```json fences before parsing. User must paste this parser code into the live n8n node.
- 2026-05-23: Full Workflow 1 recheck with n8n testing lens found remaining fragility: OpenAI can return valid-looking JSON missing `image_prompt` / arrays as strings, causing parser to mark row `Error`. Replaced parser locally with tolerant normalizer: extracts nested OpenAI output, strips fences, recovers JSON inside extra text, converts arrays/objects to Sheet-safe strings, creates fallback `image_prompt`, only hard-fails if core copy fields are missing. Local micro-test against screenshot-like output passes to `Needs Image`.


- 2026-05-25: Patched live Workflow 3 after user switched image generation to Gemini. Latest execution `8792` failed at Google Drive upload because Gemini produced binary output under an empty binary field while Drive expected `data`. Fixed live WF3: Gemini binary output now `data`, Drive upload reads `data`, Gemini node has `operation=generate` plus `aspectRatio=1:1`, image prompt explicitly requires square 1024x1024 / no horizontal composition, Drive share uses public `reader:anyone` with `allowFileDiscovery=false`, and Sheet writes direct Drive download URL. Live WF3 validates with zero errors; next manual run must verify Drive upload, public incognito URL, and actual square output.
- 2026-05-25: Local lead-generation quality pass after `bm-0062` output was polished but commercially weak. Updated `docs/content-generation-prompt.md`, both Workflow 1 exports, and Workflow 3 image prompt prep so content must include buyer triggers, Bastion & Mews credibility, soft CTA/next step, stricter 140–190 word LinkedIn target, 35–70 word Instagram enforcement, and stronger premium editorial social-ad image prompts. Parser now flags weak commercial relevance and generic trust phrasing; Workflow 3 image prompt avoids brown/generic still-life outputs and requires a clearer readiness/inspection/arrival focal point. Local JSON and code syntax checks pass; live n8n must still be updated/imported/pasted.
- 2026-05-25: Root-cause analysis of bm-0067/0068 failures. Found confirmed parser bug: /herb garnish/i matched "no herb garnish" (valid exclusion instruction in good image prompts), causing false generic_image_prompt flag. Fixed: removed herb garnish, rosemary, lemon water, blank greeting card, freestanding blank card, empty card from genericImagePatterns. Removed /For those seeking/ from tourismPatterns (false positive on CTAs). Changed forbidden_seamless_experience to forbidden_seamless (/\bseamless/i) catching seamlessly/seamless transition/etc. Added forbidden_operational_discipline, forbidden_meticulous_attention, forbidden_anticipates_needs, forbidden_fosters_trust to parser. Changed gold standard LinkedIn example opener from "A premium stay begins to earn confidence" (banned pattern the model was copying) to "The inspection happens the day before arrival" + added STRUCTURE ONLY note. Added retry mechanism to workflow: IF Quality Gate node → if score<75 → Prepare Retry Prompt → OpenAI Retry (gpt-4o, temp 0.85) → Parse Retry → Update Sheet. Both workflow JSONs updated; user must import to live n8n.
- 2026-05-25: Quality gate lowered from 90 to 75; model upgraded gpt-4o-mini→gpt-4o; temperature 0.5→0.7; system prompt hardened: banned stock openers ("A premium stay begins/crafted/defined"), added topic reframing rule (forbidden-phrase topics must be operationally reframed before writing), expanded forbidden phrases list (operational discipline, meticulous attention to detail, seamless transition/integration, fosters trust, establishing a reliable framework, etc.), replaced default Instagram example with two concrete operational alternatives, added CTA specificity rule requiring post-specific next steps not generic "operational standards" or "property management needs". Both workflow JSONs and content-generation-prompt.md updated in sync.
- 2026-05-25: Second quality pass after `bm-0063` still failed with generic phrases (`trust is often built`, `invisible details`, `seamless experience`, `hallmarks of`, `subtle yet impactful`, `support your needs`, etc.). Strengthened prompt with a gold-standard concierge/trust example, expanded parser forbidden phrases, and changed Workflow 1 so `quality_score < 75` blocks image generation (`status = Error`, blank `image_status`, explanatory `error_message`) instead of spending Workflow 3 runs on rejected copy. Workflow 3 image prompt now avoids freestanding blank greeting cards, herb/plant focal points, pen-as-main-object setups, lemon/rosemary water, and decorative beige still-lifes; it prefers flat briefing/condition-review sheets tucked under key/card as operational cues. Local JSON/code syntax checks pass.
- 2026-05-25: Fixed repeated `74` scoring after `bm-0064`. Cause was parser logic, not row scoping: any quality flag used `Math.min(baseScore, 74)`, collapsing all failures to 74. Parser now normalizes clean outputs to 90–96, applies severity-based penalties for flagged outputs, and allows image generation only when `quality_score >= 90` with empty `quality_flags`. Prompt examples were corrected so Instagram example is 35+ words and the gold-standard LinkedIn example matches 140–190 words. Local parser test: `bm-0064`-style output now scores 62 and blocks; clean 151-word LinkedIn + 37-word Instagram sample scores 91 and moves to `Needs Image`.
- 2026-05-25: Made Workflow 1 automation-level 90+ rather than manually dependent. Added `90+ output calibration` section to the brand prompt using the manually approved concierge/pre-arrival style. Updated `Prepare Retry Prompt` to embed the calibration sample and hard retry rules. Changed `IF Quality Gate` to route by `status = Needs Image` instead of raw `quality_score >= 75`, and made both first-pass and retry parsers require `quality_score >= 90` with empty `quality_flags` for image generation. Local validation: bad generic sample scores 40/Error and retry prompt includes calibration; good 152-word LinkedIn + 35-word Instagram sample scores 92/Needs Image with empty flags. Import updated `workflows/01-content-generation.json` into live n8n.
- 2026-05-25: Adjusted Workflow 1 pass threshold after user observed an 82-score row was marked Error. Root cause: parser/IF gate still required clean 90+ and blocked any flags. New behavior: deterministic `quality_score >= 70` proceeds to `Needs Image` / `Ready to Generate`; `quality_score < 70` becomes `Error` and routes to retry. `quality_flags` no longer block by themselves and remain as review warnings. IF Quality Gate now checks `Number($json.quality_score) >= 70`. Local validation: warning sample recalculated to 79 and passed; bad generic sample recalculated to 40 and routes to retry.
- 2026-05-28: Analyzed client copywriter revisions from `/Users/tamerlan/Downloads/bastion_mews_batch1.pdf` for BM-002..BM-006. Key calibration: output should be simpler and more direct, use first-person Bastion & Mews voice (`we inspect`, `we coordinate`, `we review`, `we prepare`, `we report`), start from real situations/contrasts, use plain CTAs (`we're worth talking to`, `get in touch`, `reach out`), allow Malta as practical/logistical context, and allow up to 2 restrained Instagram hashtags. Updated `docs/content-generation-prompt.md`, added `docs/bastion-mews-copywriter-calibration.md`, synced both Workflow 1 JSON exports, updated retry calibration, and adjusted parser quality gates so approved direct CTAs / 2 Instagram hashtags pass. Local validation: workflow JSON parses, code nodes compile in n8n-compatible wrapper, embedded system prompts match docs prompt, and copywriter-approved BM-004-style sample scores 94 with `Needs Image` and no quality flags.
- 2026-05-28: Second BM-002 hardening pass after generated `bm-002.2` still sounded like premium consultant copy. Added anti-consultant premium language rules to prompt/parser (`transforming it into a luxury hospitality asset`, `anticipated with precision`, `quiet precision`, `enhances guest experience`, `premium edge`, `premium standing`, `sought-after hospitality asset`, generic `discreet operations partner` CTA, etc.). Parser now adds `consultant_premium_language`, applies stronger LinkedIn/Instagram length penalties, and retry prompt tells the model to use BM-002 copywriter structure. Local validation: user-provided bad BM-002.2 sample now scores 40/Error and triggers retry; good copywriter-style BM-002 sample scores 94/Needs Image with empty flags.
- 2026-05-29: Third BM-002 hardening pass after retry still failed with `turning it into a trusted hospitality asset`, `running smoothly`, `positive experience`, `peace of mind`, `level of service`, etc. Changed prompt from general style guidance to explicit BM-002/listing archetype: mandatory paragraph jobs for LinkedIn/core, Facebook, and Instagram; do not get creative with listing rows. Added parser bans for the new failure phrases and synced both Workflow 1 exports. Lowered generation temperature to 0.38 and retry temperature to 0.42 to improve style matching over creative paraphrase. Local validation: latest bad sample scores 40/Error with detailed flags; approved BM-002-style sample scores 94/Needs Image with empty flags.
- 2026-05-29: Incorporated copywriter review of latest BM-002 (7/10). Added targeted micro-rules to prompt/parser: LinkedIn evidence lists must keep parallel grammar (`whether/whether/whether`, no `if/whether` mix), pricing phrasing should be `pricing is managed seasonally` / `pricing is adjusted to seasonal demand`, owner visibility should say `without requiring their involvement` or `without having to ask` instead of `daily noise`, avoid `efficiently`, and avoid stiff Facebook opener `There is a version...`. Parser now flags `broken_parallel_structure` plus these wording issues; LinkedIn lower bound loosened to 138 words to avoid false flag on near-perfect 139-word copy.
- 2026-05-29: Diagnosed BM-004 failure after BM-002 started scoring 92. Root cause: Workflow 1 retry prompt was overfit to BM-002/listing; generic hard rules still mentioned BM-002 for all topics, while the system prompt had no equally strong BM-004/business-guest archetype. Added BM-004 business guest archetype to `docs/content-generation-prompt.md` and retry router (`isBusinessGuestTopic`) so business-stay rows get their own paragraph jobs and Instagram structure. Added bans for BM-004 failure language: `for companies arranging premium stays`, `true measure of a successful stay`, `smoothly`, `seamless`, `efficiency/efficiently`, `elegance and efficiency`, `tailor the experience`, `unnecessary noise`. Validation: retry prompt for BM-004 includes BM-004 archetype and does not include BM-002 archetype; bad BM-004 sample scores 40/Error with relevant flags.
- 2026-05-29 correction after user pushback: per-post prompt hardening is the wrong architecture. Refactored Workflow 1 prompt/retry from BM-specific rules into reusable audience/problem archetypes: Owner / Property Stewardship, Business Guest Hosting, Investor / Pre-Decision Advisory, and Concierge / Preparation. Both `OpenAI Generate Content` and `OpenAI Retry Generate` now use the same universal `docs/content-generation-prompt.md`; working prompt/workflow JSONs no longer contain `BM-002` / `BM-004` instructions. `Prepare Retry Prompt` selects archetypes from topic/audience/source notes, not row ID. `docs/bastion-mews-copywriter-calibration.md` now marks BM examples as historical failure samples, not implementation instructions.
- 2026-05-29 audit after user tested `bm-004.2`, `bm-005.2`, `bm-003.2`, `bm-001.2` and was unhappy. Root cause: the model echoed unsafe raw brief/source-note language (`seamless`, `every detail`, `business and leisure`, `warm Mediterranean pace`, `peace of mind`, checklist wording) and the quality gate let major style failures pass as 74/82. Fixed Workflow 1 universally: added brief-sanitization rules, added Guest / Practical Malta Stay archetype, changed retry router to select exactly one dominant archetype (`bm-004` business, `bm-005` investor, `bm-003` guestStay, `bm-001` owner), shortened retry prompt, and capped major flags below 70 so these outputs retry/block instead of proceeding to image generation.
- 2026-05-29 `.3` retest audit: user reported 3/4 pass and 1 error, but Sheet rows still showed old retry prompt text (`Selected reusable archetype(s)` plus long approved samples) and old scoring behavior (`forbidden_seamless` passing as 82). Conclusion: live n8n was not running the latest local Workflow 1. Added version stamp `bm-universal-2026-05-29-v2` to system prompt, first-pass user prompt, and retry prompt so future generated rows can prove which prompt version ran. Local Workflow 1 remains validated with short dominant-archetype retry and major-flag cap below 70.
- 2026-05-29 full `.3` failure audit after user updated Workflow 1: all four rows (`bm-004.3`, `bm-005.3`, `bm-003.3`, `bm-001.3`) failed under `bm-universal-2026-05-29-v2`. Root cause: prompt contamination from long approved samples (BM-004 copied the business-guest sample nearly verbatim), excessive negative/example text, retry preserving unsafe brief language, and a gate that treated every major style flag as sub-70 Error. Implemented local v3: removed long copy samples from `docs/content-generation-prompt.md`, kept only structural calibration, added anti-copy retry rules, added parser `sample_copying` detection, changed IF gate to pass only `status = Needs Image`, changed first pass to retry anything not clean 85+/empty flags, and changed retry outputs so sub-70 remains `Error` while 70-84 reviewable copy becomes `Needs Review` instead of false failure. Local JSON and Code node syntax checks pass; sample-copy test blocks, clean 140-word sample scores 92/Needs Image. Live n8n still showed v2 in `n8n_get_workflow` during audit and must be updated/imported to v3 before retesting.
- 2026-05-29 v5 redesign (last-attempt mandate from user): after `bm-004.4` again scored 40/Error under v4 (consultant opener "...involves more than just choosing a property. It's about ensuring..."), confirmed the regex-blocklist architecture punishes the model's default register without teaching, and the gate's zero-flag requirement meant clean copy never auto-approved. Rebuilt Workflow 1 to v5 (`bm-universal-2026-05-29-v5`): slimmed system prompt 43.6K→10.4K chars, examples-led (the 5 copywriter-approved A–E posts presented as "the standard, match this voice") + ~6 distilled principles + opening rule + short instant-fail phrase list + per-channel word counts + JSON contract. Both parser Code nodes rebuilt with a blocking-vs-soft gate: blockingFlags (instant-fail phrases/tourism/checklist, -22 each), softFlags (consultant tells + length/hashtag/specificity, -4..-8), removed double-counting composite `consultant_premium_language`, `score = 95 - blocking*22 - soft` (floor 30, cap 96, min 92 if clean), `clean = score>=80 && !hasBlocking && lengthOk(LinkedIn 130-200w)`. **Routing is now `clean ? 'Needs Image' : 'Needs Review'` — NEVER 'Error' for quality; Error only in catch (parse/API failure).** Rationale: human-in-the-loop review is correct for a lead-gen pipeline, not a dead-end. Local verification: JSON valid (13 nodes), all 4 stamps v5, IF gate still `status equals "Needs Image"`, both parsers' only `status:'Error'` is in catch block. n8n MCP read/write is BLOCKED (`availableInMCP: false` for `6DwmwIbWgFholDMr`) — user must import `workflows/01-content-generation.json` manually (or enable MCP), reset a row to `Ready to Generate` suffix `.5`, and run to confirm Needs Image/Needs Review (not Error).
- 2026-05-29 v6 (after user re-ran under v5 → bm-004.4 scored 53, wants ~80-90 average): confirmed 53 is an HONEST gate score. The generated body was on-voice ("we inspect... we coordinate... we handle requests discreetly") but it OPENED with the consultant template ("Hosting a business guest requires more than just selecting a property. It involves...") and CLOSED with "provides a seamless experience for the guest and peace of mind for the host." v5 already banned both "more than just" and "seamless" — so the lesson is that more negative rules do NOT work; gpt-4o's B2B prior overrides a buried banlist, and failures concentrate at the opening + closing. v6 keeps the 5 examples verbatim and rebuilds the rule tail: four positive "how to write" moves with concrete opening formulas, a **mandatory pre-output self-revision pass** (re-read own opening/closing & rewrite if bad, word-scan every field for AI-tells, length-check, emit JSON only after passing), and a compressed hard-ban tail. The quality gate was deliberately left strict and UNCHANGED (did not soften to inflate scores; the honest path to an 80-90 average is clean generation, not a looser thermometer). Verified parser `existing` flags come from the model's own JSON, not inherited from the sheet. Build script `/tmp/build-v6.js`; local verify: JSON valid, 4 v6 stamps, 0 v5 leftovers, examples A–E preserved, self-revision section present. Still requires manual import to live `6DwmwIbWgFholDMr`.
- 2026-05-29 v7 (after bm-004.5 ran under v6 → 63/Needs Review): real PROGRESS — v6's positive opening formulas killed the consultant opening (opener became "A business guest's experience starts before they even arrive."). Remaining issues were narrow and stubborn: one stray word "seamless" (a -22 BLOCKING flag) and short length (LinkedIn 94 words vs 140-185, IG 16 vs 35-70). Conclusion: gpt-4o won't reliably self-delete a banned word or hit word counts from prompt text alone, so move it to CODE. v7 adds (1) a deterministic `sanitizeText()` term-filter in BOTH parsers, applied to all fields before scoring, replacing the fixed AI-tell set with neutral equivalents (seamless→well-prepared, peace of mind→confidence, etc.) and logging an auto-correction note; (2) a forceful count-based length step in the prompt self-revision (count words, expand with concrete detail to 150-175) + move-2 now asks for 5-7 concrete operational details. Proven by local gate simulation: real bm-004.5 retry under v7 → seamless removed, no blocking, score 63→85 (still Needs Review purely due to 94-word length); a 135-word clean version → 95, zero flags, auto-approve Needs Image. So the word problem is solved deterministically; the only remaining variable is whether the model writes ≥130 words. Gate thresholds left strict/unchanged. Build `/tmp/build-v7.js` (run after build-v6 on top of v5). Still requires manual import to live `6DwmwIbWgFholDMr`.
- 2026-06-01 v8 after copywriter reviewed `.5` tests: scores were bm-001.5 4/10, bm-003.5 5.5/10, bm-004.5 5.5/10, bm-005.5 7.5/10. Root issue: v7 over-scored drafts that a human copywriter would reject; remaining failure mode is not row-specific but platform/editorial: short LinkedIn/Instagram/Facebook/X, Core/LinkedIn duplication, generic comfort claims (`impeccable condition`, `memorable experience`, `smooth and uninterrupted`, `on standby`), weak CTAs (`let's talk`, `let's discuss your investment`, `how we can assist`), and repeated `protect your investment`. Built v8 (`bm-universal-2026-06-01-v8`) in local Workflow 1 exports and docs: strict platform lengths, Core/LinkedIn similarity detection, weak CTA/repetition/generic claim flags, stronger human-aligned scoring, auto-approval only when score >=88 and `quality_flags` empty. Added `docs/bm-v5-copywriter-audit.md`. Local code-node syntax checks pass for both workflow JSONs; simulated scoring now aligns roughly with copywriter: bm-001-style 40, bm-004-style 49, bm-005-style 71, bm-003-style routes Needs Review with tourism/comfort flags.
- 2026-06-01 v9 full prompt audit after `bm-004.6` scored 51. Confirmed live row and n8n draft were still stamped `bm-universal-2026-06-01-v8`, with long full-post examples, inconsistent first-pass word counts, old retry archetype prompt, and parser `sanitizeText()` auto-replacements. Root cause is universal prompt architecture, not BM-004 specifically: long examples encouraged copying, banlist-heavy retry did not force channel length/platform separation, and sanitization masked bad copy. Built local v9 (`bm-universal-2026-06-01-v9`) in `docs/content-generation-prompt.md`, `workflows/01-content-generation.json`, and `workflows/Michael Dang - 01 Content Generation.json`: removed long examples, added reader-job model, required LinkedIn structure, unified platform contracts, removed parser auto-sanitization, strengthened retry with previous length counts and concrete repair moves, and added `docs/prompt-audit-2026-06-01-v9.md`. Local validation: JSON parses, Code nodes compile, no v8/sanitize leftovers, bad bm-004.6 sample scores 30/Needs Review with precise flags, clean bm-004-style sample scores 96/Needs Image with no flags. Live n8n still must be updated/imported to v9 before retesting.
- 2026-06-01 v10 copywriter-gold update: user supplied final requirement to use copywriter-approved revised Batch 1 PDF as canonical voice and keep the workflow universal. `/mnt/data/bastion_mews_batch1.pdf` was unavailable in this local app environment, so `/Users/tamerlan/Downloads/bastion_mews_batch1.pdf` was used. Updated Workflow 1 exports and docs to `bm-universal-2026-06-01-v10-copywriter-gold`: prompt now distills approved copywriter patterns without BM-specific overfitting, enforces real-situation openings, operator voice, woven operational proof, calibrated CTAs, platform lengths (Core 95-160, LinkedIn 145-190, Facebook 90-150, Instagram 40-65, X 180-260 chars), and preparation-scene image prompts. Parser adds `not_bastion_mews_voice`, `definition_style_opening`, `checklist_tone`, `weak_cta`, strict length/similarity checks, and controlled one-shot repair (`v10-copywriter-gold-repair`) before manual review. Local validation: both Workflow 1 JSONs parse, code nodes compile, bad BM-004-style sample scores 30/Needs Review with repair prompt, good BM-004-style sample scores 96/Needs Image with empty flags. Live n8n still needs import/paste of the updated Workflow 1 file before retesting.
- 2026-06-01 v11 archetype-guided update after live `bm-004.6` still failed under v10 repair. Updated Workflow 1 exports and docs to `bm-universal-2026-06-01-v11-archetype-guided`: first-pass generation now selects one universal archetype (`business_guest_hosting`, `property_management_asset`, `investor_operational_questions`, `concierge_operations`) from row text, forces `business_guest_hosting` for `bm-004`/business guest rows, passes the selected archetype into the OpenAI prompt, preserves it through `_retry_archetype`, and includes the mandatory archetype structure in repair prompts. Parser independently infers archetype and adds `wrong_archetype_structure` if LinkedIn does not follow it; hard bans now include `composed and elegant environment` and `successful stay`. Local validation: both Workflow 1 JSONs parse, code nodes compile, bad BM-004-style draft scores 30/Needs Review with `wrong_archetype_structure` and repair prompt, clean BM-004 archetype sample scores 96/Needs Image with empty flags. Live n8n must be updated/imported to v11 before retesting.
- 2026-06-01 v12 deterministic archetype router after user clarified v11 was still too topic-specific. Updated Workflow 1 exports and docs to `bm-universal-2026-06-01-v12-deterministic-archetype-router`: archetype selection now prioritizes `content_pillar` mapping, uses keyword fallback only when pillar is missing/unknown, ignores stale `_retry_archetype` when it conflicts, stores the corrected result in `selected_archetype`, and logs `archetype_corrected_from_retry` only as diagnostics. Removed BM-004 acceptance wording from operator docs and reframed tests as universal one-row-per-archetype checks. Local validation: both Workflow 1 JSON files parse, key Code nodes compile, all four clean archetype simulations route correctly and score 96/Needs Image with empty flags, and stale retry `business_guest_hosting` on `owner_investor_advisory` is corrected to `investor_operational_questions`. Live n8n must be updated/imported to v12 before retesting.
- 2026-06-02 v13 intent-conflict guard after user pasted `bm-005.6` score 43 and `bm-003.6` score 49. Root causes: `Prepare Retry Prompt` was still weaker than parser-generated `repair_prompt`, had a bad word-count regex (`/s+/` instead of `/\s+/`), trusted stale retry/selected archetype too much, and did not handle rows where `content_pillar` conflicts with topic/audience/CTA. Updated Workflow 1 exports and docs to `bm-universal-2026-06-02-v13-intent-conflict-guard`: added `guest_stay_experience` archetype for guest-facing Malta stay topics, added `archetype_corrected_from_pillar_conflict`, made `Prepare Retry Prompt` use the same deterministic classifier as the parser, strengthened retry length blueprint (LinkedIn 4 short paragraphs / 155-175 words, Core 105-135, Facebook 95-125, Instagram 45-60, X 190-240), and required first-person Bastion & Mews actions especially for investor/advisory rows. Local validation: both Workflow 1 JSON exports parse, `Parse and Validate JSON`, `Prepare Retry Prompt`, and `Parse Retry Response` compile; `bm-005.6` repair now selects `investor_operational_questions` with correct previous lengths; `bm-003.6` repair now selects `guest_stay_experience` and logs both stale retry + pillar conflict correction; clean investor and guest-stay simulations score 96/Needs Image with empty flags. Live n8n must be updated/imported to v13 before retesting.
- 2026-06-02 v14 depth/score calibration after user reported all live tests still showing `30`. Checked the live Google Sheet `Michael Dang` / `content_calendar`; `bm-004.6` was confirmed at `quality_score = 30`. Root cause: weak short copy plus the scorer's additive penalties collapsed different failures into the hard floor `Math.max(30, ...)`, similar to the earlier repeated-74 issue. Updated Workflow 1 exports and docs to `bm-universal-2026-06-02-v14-depth-and-score-calibration`: added explicit depth requirements to first-pass and repair prompts, synced retry system prompt to the canonical five-archetype prompt, capped grouped length penalties, lowered the diagnostic floor to 10, and loosened business-hosting archetype recognition for "property is just the beginning". Local validation: both Workflow 1 JSON exports parse and Code nodes compile; the `bm-004.6` text without stale saved flags now recalculates to 42 instead of 30. Live n8n still must be updated/imported to v14 before retesting.
- 2026-06-02 v15 brief-contamination patch after user reported live `bm-003.6` scored 13. Reclassified the issue: v14 exposed a low raw score, but the main failure was source-note contamination (`unique blend`, `business/leisure`, `ideal`, `seamless`, `smooth`) and tourist image direction (`coastline`, heritage scenery), not merely lack of depth. Updated local Workflow 1 exports/docs to `bm-universal-2026-06-02-v15-brief-sanitizer-repair-first` and patched live n8n Workflow 1 (`6DwmwIbWgFholDMr`) via MCP. v15 adds unsafe brief sanitization to first-pass and repair prompts, flags coastline/postcard/heritage-architecture image prompts, and recalibrates bad-copy scoring so failed drafts remain blocked without absurd public scores like 13. Local JSON parses and Code nodes compile; pasted `bm-003.6` re-scores to 20 with `generic_image_prompt`. n8n static validator still reports Code-node analysis errors but connections are valid; next live UI run must confirm `_attempt = 2` when first pass fails.
- 2026-06-02 template-adaptation pivot after user rejected continued v15/v16 complexity. Workflow 1 is now a copywriter-approved template adaptation system (`bm-template-adaptation-2026-06-02`), not open-ended generation/router/sanitizer escalation. It selects one BM template family (`BM-002` property management asset, `BM-003` guest stay experience, `BM-004` business guest hosting, `BM-005` investor operational questions, `BM-006` concierge operations), injects the approved structure/rhythm, tells the model not to write from scratch, and repairs once using the same selected template. Important correction: BM-003 overrides stale `luxury_property_management` pillar when topic/audience clearly indicate guest stay experience. Local acceptance simulation: 5/5 clean rows selected the expected template and scored 94/Needs Image; bad travel/luxury sample scored 35/Needs Review with blank `image_status`. Updated local Workflow 1 exports, `docs/content-generation-prompt.md`, sheet/CSV schema with `selected_template`, workflow notes, and smoke report. Patched live n8n Workflow 1 (`6DwmwIbWgFholDMr`) via MCP. Live n8n validation still shows static Code-node analyzer errors but connections are valid (`15` valid, `0` invalid). Google Sheets metadata check hit 429, so confirm/add live `selected_template` column after cooldown; parser also writes `selected_archetype = selected_template` as a fallback for current sheet visibility.
- 2026-06-02 Workflow 1 Lite rollback after user said template/archetype routing still over-engineered and scores fell to 20-35. Local Workflow 1 exports and docs now use `bm-workflow1-lite-2026-06-02`: approved BM-002..BM-006 examples are only voice references, production routing is a lightweight `content_type` (`property_management`, `guest_stay`, `business_guest_hosting`, `investor_advisory`, `concierge_operations`), and `selected_template`, `selected_archetype`, `_retry_archetype`, `_retry_user_message`, `repair_prompt`, and `wrong_template_structure` no longer control generation or scoring. Automatic repair loop is disabled (`IF Quality Gate`, `Prepare Retry Prompt`, `OpenAI Retry Generate`, `Parse Retry Response` disabled and disconnected); parser writes directly to the sheet update node. Quality gate is softer: `80+` -> `Needs Image`, `65-79` -> `Needs Review`, below `65` -> `Needs Review`, with hard low scores reserved for banned filler, wrong topic, brochure copy, no operational proof, broken JSON, or empty platform fields. Local acceptance simulation: 5/5 clean rows scored `65+` as usable drafts; bad filler/travel sample stayed `Needs Review` with blank `image_status`. Live n8n was not patched during this Lite rollback; import/paste the updated local Workflow 1 before live retesting.
- 2026-06-02 Workflow 1 Lite v2 score-floor fix after user reported all live tests still at `45`. Root causes: live workflow was still on first Lite stamp; stale generated `content_type` could override obvious row meaning (`bm-004.6` stayed `concierge_operations` despite business-hosting topic/pillar/audience); parser inherited old/model-supplied `quality_flags`; one hard-banned filler plus short platform fields collapsed to the floor. Updated local exports/docs and patched live n8n Workflow 1 (`6DwmwIbWgFholDMr`) to `bm-workflow1-lite-2026-06-02-v2`: topic/audience/pillar can override stale `content_type`, business-hosting intent routes before concierge keywords, parser replaces hard-banned filler before scoring, ignores old/model flags, and caps length penalties. Pasted `bm-004.6` output recalculates to `68 / Needs Review`, corrects `content_type` to `business_guest_hosting`, and removes `seamless`; it still does not route to image generation because the draft is short and visual direction is a graphic card. Live Workflow 1 is currently `active: false` but manual UI runs should use the saved draft.
- 2026-06-02 Workflow 1 Lite v3 repeated-55 fix after user asked to check latest `.6` tests in the sheet. Direct sheet check found `bm-004.6 = 55`, `bm-005.6 = 55`, `bm-003.6 = 55`, `bm-001.6 = 58`. Root causes: v2 recreated a repeated-score floor via `Math.max(55, ...)`; operational proof regex missed normal inflections (`inspects`, `prepares`, `handling`, `coordination`, `management`); brochure flag matched image prompt text like `not a travel postcard`; and owner/investor routing could misclassify property-management owner rows. Patched local exports/docs and live n8n Workflow 1 (`6DwmwIbWgFholDMr`) to `bm-workflow1-lite-2026-06-02-v3`: diagnostic floor is now 25, operational regex accepts inflected/action nouns, brochure check uses copy fields only, negated postcard image prompts are ignored, and `owner_investor_advisory`/buying-renting rows route to `investor_advisory` while luxury-property-management owner rows route to `property_management`. Existing sheet rows do not recalculate automatically; rerun/reset rows to see v3 scores.
- 2026-06-02 Workflow 3 visual strategy fix after user reviewed `.7` image tests and found them too boring/samey. Checked Sheet rows `bm-004.7`, `bm-005.7`, `bm-003.7`, and `bm-001.7`; all generated the same beige/limestone still-life formula (blank sheet/card, key/card, folded linen, notebook, natural light), so the images were polished but commercially meaningless. Updated local `workflows/03-image-generation.json` and live n8n Workflow 3 (`izx5FsKxTOdGJsVB`) to `bm-image-visual-territories-2026-06-02-v1`: `Prepare Image Prompt` now maps rows to visual territories by `content_type`/intent (`business_guest_hosting`, `investor_advisory`, `property_management`, `guest_stay`, `concierge_operations`), chooses deterministic scene variants, allows cropped no-face service action, and explicitly bans repeating the writing-desk + blank sheet + key/card + folded linen batch formula. Also set live/local Google Drive upload node `operation = upload`; live Workflow 3 validates with `0` errors and `15` valid connections. Old `.7` images will not change automatically; reset image fields or duplicate rows for a fresh `.8` image test.
- 2026-06-02 Workflow 2 Buffer Meta post type fix after Buffer returned `Invalid post: Facebook posts require a type (post, story, or reel)` and the same for Instagram while X scheduled successfully. First attempted top-level `type: post`, but Buffer returned `Field "type" is not defined by type "CreatePostInput"`; official Buffer docs show `CreatePostInput` has `metadata`, and Facebook/Instagram post type belongs inside channel-specific metadata. Corrected local `workflows/02-buffer-scheduling.json`, `workflows/Michael Dang - 02 Buffer Scheduling.json`, and live n8n Workflow 2 (`njxWxoD6cv4KHi5O`): Facebook now sends `metadata: { facebook: { type: post } }`; Instagram sends `metadata: { instagram: { type: post, shouldShareToFeed: true } }`; X remains unchanged. The n8n `jsonBody` expression uses string concatenation instead of template literals so validation passes. Added rerun protection: platforms with existing `buffer_<platform>_post_id` or scheduled/sent/queued/published/duplicate-recently status are skipped, and Buffer's "posted that one recently" response is normalized to `Duplicate Recently - already protected by Buffer` rather than treated as a failure. Live Workflow 2 validates with `0` errors and `13` valid connections.
- 2026-06-03: Client asked whether the system can produce branded posts after a live Instagram test. Strategic recommendation: yes, but do not rely on AI image generation to render exact text/logos. Use a two-layer visual pipeline: generated editorial photo/scene from Workflow 3, then deterministic branded composition layer (logo/wordmark, small category label, optional short headline or pull quote, border/safe-zone treatment, export formats). This can become Workflow 4 / Brand Composer before Buffer scheduling, while Workflow 3 remains responsible only for no-text/no-logo imagery.
- 2026-06-03: Added manual topic/planning assets so the operator does not need to invent each row from scratch: `docs/bastion-mews-topic-bank.md` with reusable topics across `property_management`, `guest_stay`, `business_guest_hosting`, `investor_advisory`, and `concierge_operations`; `docs/bastion-mews-topic-starter-pack.csv` with 12 ready-to-copy Sheet rows; and `docs/bastion-mews-branded-post-system.md` defining branded formats (`editorial_mark`, `headline_overlay`, `insight_card`, `carousel_cover`) plus recommended future Workflow 4 fields.
- 2026-06-04: Michael shared reference screenshots showing bold Canva/Envato-style social media template packs (large typography, dark/green gradients, phone/feed mockups, repeated card layouts). Created a local client-reference proof direction at `/Users/tamerlan/Desktop/michaeldang/outputs/client-reference-pack/` with 6 square PNG concepts plus `overview-pack.png`, `review.html`, and `manifest.json`. Added generator `scripts/generate-client-reference-pack.py` and notes `docs/client-reference-pack-notes.md`. This shifts the likely branded-post direction from subtle editorial/luxury photo overlays toward a stronger graphic template system for client approval.
- 2026-06-04: After user approved the client-reference direction, implemented it into the actual branded composer layer. Rewrote `scripts/brand-composer/server.js` to produce bold template-pack cards (`client-reference-pack-2026-06-04`) using large typography, dark navy/green gradients, lime/gold accents, repeated card layouts, phone/feed mockup cues, and generated image texture. Added/validated `workflows/04-branded-post-composer.json`, which reads `Needs Brand` rows, calls `http://localhost:3456/compose`, uploads branded PNGs to Google Drive, shares them, and writes `brand_status = Branded` + `branded_image_url`. Local smoke test passed: `npm install` clean, `node -c` clean, `/health` OK, `/compose` returned valid 1080x1080 PNG at `outputs/client-reference-pack/composer-local-test.png`; all workflow JSON and CSV header checks passed. Live n8n still needs import/credential binding/real row acceptance.

## Stack
- n8n
- Google Sheets
- Google Drive
- OpenAI API
- Buffer GraphQL API

## Important Context
- Client: Michael Dang.
- Brand: Bastion & Mews.
- Niche: refined 5-star boutique stays, luxury property management in Malta, business/luxury stays, owner/investor advisory, concierge operations.
- MVP must stay simple and reliable because budget is $200.
- Buffer is the required publishing layer; avoid direct platform APIs.
- OpenAI API key was shared insecurely and must be rotated before live use; never hardcode it.
- Buffer channels: Facebook/Instagram/X connected; LinkedIn pending verification.

## Decisions
- 2026-05-18: Use Google Sheets rather than Airtable for the MVP review queue.
- Why: cheaper, faster to hand over, and sufficient for one editorial approval flow.
- 2026-05-18: Use Buffer as the publishing layer; preferred mode is `addToQueue`, with `customScheduled` only when an exact date/time is present.
- Why: avoids direct social APIs and keeps operations inside the client's Buffer account.

## Open Tasks
- [ ] 2026-05-25: Finish and improve image generation automation: run one real `Needs Image` row through Workflow 3, verify OpenAI image -> Drive upload -> public direct URL -> Sheet update, then tune image prompt / quality gate if output is still generic.
- [ ] 2026-05-25: Import/paste the latest local Workflow 1 and Workflow 3 changes into live n8n, then rerun `bm-0062` or a fresh row to verify improved lead-oriented copy and stronger image output.
- [ ] 2026-05-28: Import/paste the updated copywriter-calibrated Workflow 1 into live n8n, regenerate one fresh row, and compare against `docs/bastion-mews-copywriter-calibration.md` before scaling.
- [ ] 2026-05-29: Import/paste Workflow 1 v3 into live n8n, verify the first-pass user prompt and retry prompt show `bm-universal-2026-05-29-v3`, then rerun one `.4` test row before scaling to four rows.
- [ ] 2026-06-01: Import/paste Workflow 1 v8 into live n8n, verify generated rows show `bm-universal-2026-06-01-v8`, then rerun one `.6` row first; only scale to all four if the first row lands `Needs Image` or a human-plausible `Needs Review` with specific flags.
- [ ] 2026-06-01: Import/paste Workflow 1 v9 into live n8n, verify generated rows show `bm-universal-2026-06-01-v9` in first-pass prompt and `_retry_user_message`, then rerun one `.7` row before scaling to the four-row batch.
- [ ] 2026-06-02: Import/paste Workflow 1 v14 into live n8n, verify generated rows show `bm-universal-2026-06-02-v14-depth-and-score-calibration` / `...-repair`, then rerun one row and confirm weak rows no longer all collapse to `30`.
- [ ] 2026-06-02: Run one full live Workflow 1 test under v15 from the n8n UI. Confirm first-pass failures route through repair (`_attempt = 2`) and generated rows show `bm-universal-2026-06-02-v15-brief-sanitizer-repair-first` / `...-repair`.
- [ ] 2026-06-02: Run Workflow 1 Lite v3 (`bm-workflow1-lite-2026-06-02-v3`) from the n8n UI on 5 clean rows, one per `content_type`; expect at least 3/5 `quality_score >= 65`, no wrong template/archetype blocking, weak rows `Needs Review`, no repeated `45`/`55` floor, and no automatic repair loop.
- [ ] 2026-06-02: Confirm/add the live `content_type` column in `content_calendar`; `selected_template` is no longer required for Workflow 1 Lite.
- [ ] 2026-06-02: Run a fresh Workflow 3 image batch under `bm-image-visual-territories-2026-06-02-v1` by duplicating/resetting the `.7` rows as `.8`; expect at least 4/5 images to be visually distinct, no more than one beige tabletop still-life, and each image to communicate its row's visual territory.
- [ ] 2026-06-02: Rerun the partially failed Workflow 2 row where X already scheduled and Facebook/Instagram failed; confirm Workflow 2 skips existing X and sends Meta posts with `type: post`.
- [ ] 2026-06-03: Define a Bastion & Mews branded post system: 2-3 square/safe Instagram templates, deterministic overlay rules, logo/wordmark asset source, typography/colors, and whether posts should be single-image branded cards, carousels, or lightly branded editorial photos.
- [ ] 2026-06-03: Add the optional Sheet columns from `docs/bastion-mews-branded-post-system.md` when ready: `branded_format`, `overlay_headline`, `overlay_label`, `brand_status`, `branded_image_url`, and `branded_image_drive_file_id`.
- [ ] 2026-06-03: Build first local branded proof pack from `docs/bastion-mews-topic-starter-pack.csv`: 3 square templates, 6 sample topics, exported PNG previews, and a contact sheet for client review.
- [ ] 2026-06-04: Send/show `/Users/tamerlan/Desktop/michaeldang/outputs/client-reference-pack/overview-pack.png` to Michael and ask whether this bold template-pack direction is closer to his reference. If approved, convert the direction into 3-4 production templates for Workflow 4.
- [ ] 2026-06-04: Import `workflows/04-branded-post-composer.json` into live n8n, replace Sheet/Drive placeholders, bind Google credentials, start `scripts/brand-composer/server.js`, and run one real `image_status = Image Generated` row through Workflow 4. Confirm `branded_image_url` opens publicly and then run Workflow 2 to verify Buffer receives the branded image.
- [ ] Rotate OpenAI key and create/bind n8n OpenAI credential
- [ ] Create Google Sheet from `docs/bastion-mews-content-calendar-template.csv` or `.xlsx`
- [ ] Create Google Drive folder for generated images and capture folder ID
- [ ] Import workflows 1, 2, and 3 into the authenticated target n8n account
- [ ] Bind real Google Sheets, Google Drive, OpenAI, and Buffer credentials
- [ ] Paste real Facebook/Instagram/X Buffer channel IDs into `Set Buffer Config`; leave LinkedIn blank until verified
- [ ] Run first live test: 1 row through content -> image -> review -> Buffer, then expand to 3 generated rows

## Blockers
- Local n8n instance is reachable, but authenticated API access was unavailable during the 2026-05-18 pass (`/api/v1/workflows` returned 401 without `X-N8N-API-KEY`).
- Real Google Sheet ID, Google Drive folder ID, and n8n credentials are not connected yet.
- Buffer API access / API key availability must be confirmed with the client organization owner.
- Live import/execution remains untested because authenticated target n8n access and real service credentials are still missing.

## Next Steps
- Get client access and credentials.
- Import workflows into n8n.
- Test with the sample calendar row and first approved content batch.
- 2026-05-23: Quality hardening pass for Workflow 1 after live output sounded generic/salesy. Updated `docs/content-generation-prompt.md` and both Workflow 1 JSON exports with stricter Bastion & Mews voice, forbidden weak phrases, platform-specific rules, stronger image prompt requirements, `quality_flags`, and a parser-level quality gate that caps `quality_score` below 75 when forbidden phrases, generic hashtags, weak specificity, or LinkedIn/Instagram fit issues appear. Added `quality_flags` to CSV/XLSX/App Script schema and documented test rows/good-output expectations in workflow notes and smoke report.
- 2026-05-24: Second content-quality hardening pass after output still sounded like Malta travel/hotel brochure copy. Updated Workflow 1 prompt and parser gate to prohibit generic tourism language (`Malta’s unique blend`, `ideal destination`, `business and leisure`, `stunning views`, `rich cultural tapestry`, `Mediterranean pace`, etc.), reframe Bastion & Mews as a discreet premium residence operator, require operational/advisory insight in every post, require expert-observation → why-it-matters → concrete-details → calm-conclusion structure, and add `generic_tourism_language` flag capped at `quality_score <= 70`. Locally simulated bad vs good framing; bad tourism copy is flagged, good operational business-stay framing passes.
- 2026-05-24: Third content-quality pass after output became too SOP/checklist-like and images too generic. Updated Workflow 1 prompt and parser to require editorial operator prose rather than default bullets/numbered lists, added `checklist_tone` and `generic_image_prompt` quality flags, strengthened Instagram style toward short atmospheric operational lines, and required image prompts to show a preparation/operational moment (writing desk, itinerary card with no readable text, key/card, water glass, folded linen, quiet workspace, Malta limestone/heritage detail, natural light) rather than generic hotel/interior scenes. Local simulation confirms checklist/SOP copy and generic image prompts are flagged while editorial business-stay framing passes.
- 2026-05-24: Live n8n MCP patch completed for image-generation failure and image-quality direction. Workflow 3 (`izx5FsKxTOdGJsVB`) was failing because live OpenAI image node was underconfigured, Drive upload/share nodes were misconfigured, and Sheet update used the wrong update mode. Patched live Workflow 3: `gpt-image-1` image generation with binary `data`, Drive upload, public `reader:anyone` share, direct row `appendOrUpdate` by `id`, and stronger editorial/operational still-life prompt builder. Patched live Workflow 1 (`6DwmwIbWgFholDMr`): replaced deprecated OpenAI `continueOnFail`, strengthened image prompt rules, and refreshed Google Sheets schema to keep `quality_flags` / `improvement_notes` aligned. Both workflows validate with zero errors; remaining warnings are non-blocking. Next: manually run one real `Needs Image` row through Workflow 3 and verify Drive URL opens publicly.
- 2026-05-24: Attempted MCP-only execution testing for WF1 and WF3. n8n MCP cannot trigger Manual/Schedule-only workflows directly; temporarily added a webhook to WF1 for testing, but n8n Cloud blocked execution at trigger with `Execution limit reached`. Removed temporary webhook and deactivated WF1. WF1 and WF3 still validate with zero errors. Current blocker: n8n Cloud execution quota/plan limit must be cleared before real live tests can continue.
- 2026-05-24: Diagnosed WF3 latest execution `8522`: workflow showed success but skipped OpenAI because row `bm-005` had stale `image_status = Image Error`; `IF Image Prompt Valid` checked image_status and routed to error branch. Earlier `8521` failed on stale Google Sheets schema. Patched WF3 live: filter skips stale Image Error rows unless manually reset to `Ready to Generate`, prompt prep runs per item, IF checks `final_image_prompt`, OpenAI image node restored to `operation=generate` with binary output `data`, Drive upload/share restored, and WF3 validates with zero errors.
- 2026-05-24: Diagnosed WF3 execution `8524`: OpenAI image generation and Drive upload succeeded, but `Share Drive File` failed with Google Drive 400 because permission payload had empty `role/type`. Patched live share node to `reader:anyone`. Also reverted Drive upload `name` to simple expression string because resource-locator format caused uploaded files to be named `Untitled` at runtime. WF3 validates with zero errors; one non-blocking filename warning remains by design.
- 2026-05-24: Resolved recurring WF3 Share Drive File issue root cause. n8n Google Drive node validator accepted `permissionsValues` as an array, but this n8n runtime expects object shape; array patches normalized to empty `role/type`, causing repeated Google Drive 400 errors. Patched live WF3 to `permissionsUi.permissionsValues = { role: "reader", type: "anyone" }` and restored complete OpenAI image/upload params. Verified live workflow stores the corrected object shape and validates with zero errors. Keep upload filename as simple expression despite warning because resource-locator format caused `Untitled` files at runtime.
