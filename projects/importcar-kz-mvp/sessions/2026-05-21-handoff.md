# Handoff 2026-05-21 — Calculator QA & Hardening Complete

## Related
- [[overview]]
- [[current-state]]
- [[next-steps]]
- [[decisions]]
- [[risks]]

---

## State for the next agent

**Project**: ImportCar.kz MVP — `/Users/tamerlan/Desktop/importcar-kz-mvp`
**Stack**: React 19 + TypeScript + Vite + Supabase. State-based routing (no React Router). PWA.
**Build**: `npm run lint` ✅ `npm run build` ✅ 460.99 kB. Zero errors.

---

## What was completed today

Two tasks in two sessions (context limit hit mid-session; resumed cleanly):

### Task 1: Production Calculator v1 (v0.1 milestone) ✅
- Rebuilt `CalculatorScreen.tsx` from scratch: hero header, 6-field form, result card with KZT total + USD, 7-line breakdown, explainability accordion, legal disclaimer, lead capture form
- Changed default tab from `catalog` → `calculator` (in `App.tsx`)
- Added `RequestScreen.tsx` (4th tab, replaces ProfileScreen)
- Updated `BottomNav.tsx`: tab order Калькулятор / Каталог / Избранное / Заявка
- Lead form saves full calc snapshot to `metadata JSONB` in Supabase `leads` table
- `AdminLeads.tsx` shows calculator context chips from snapshot
- Schema migration appended to `supabase/schema.sql` (must be run manually in Supabase dashboard)

### Task 2: QA & Hardening ✅
Key fixes made:

**`src/components/CalculatorScreen.tsx`**
- Added `calculatedAt` state (ISO string, set at button click time, NOT at submit time)
- Race condition fix: `handleLeadSubmit` captures `const currentCost = cost` at start of async before awaits
- Snapshot fields: `source_country` (not `country`), `fuel_type` (not `engine_type`), `destination_country: 'Kazakhstan'`, `result_total_usd`, `calculated_at`
- Disabled-button hint: "Заполните цену, год и объём двигателя для расчёта."

**`src/components/AdminLeads.tsx`**
- `CalcSnapshot` type updated: `source_country`, `fuel_type`, `result_total_kzt`, `result_total_usd`, `calculated_at`
- `CalcContext` component accesses `snap.source_country` and `snap.fuel_type`

**`src/components/ImporterCard.tsx`** — full Russification:
- "Verified importer" → "Проверено"
- "Request" → "Запросить"
- "Details" / "Hide details" → "Подробнее" / "Скрыть детали"
- Detail panel: all labels Russian

**`src/components/ImporterList.tsx`**
- "Concierge partners" eyebrow → "Партнёры-импортёры"

**`src/components/LeadForm.tsx`**
- "Lead form" eyebrow → "Заявка"
- "Request sent" eyebrow → "Отправлено"

**`src/App.css`**
- Added `.calcInputHint` (13px gray, centered, mt 8px)
- `overflow-wrap: anywhere` on `.calcResultV2Total`
- `word-break: break-all` on `.adminCarLink`

**`scripts/smoke-test.mjs`** — rewritten (old script was broken, referenced non-existent English text):
- Checks "Рассчитайте стоимость авто под ключ" is visible as default
- Clicks "Рассчитать стоимость", checks `.calcResultV2Total` appears
- Mobile (< 720px): navigates `.bottomNav` → Каталог, counts cards, opens detail, clicks "Запросить", checks `.leadModal`

**`scripts/calc-sanity.mjs`** — new file, 5 Playwright pricing sanity scenarios:
```
Korea  / 80M KRW / 2023 / 2998cc / petrol   → 38,190,000 ₸
USA    / 40k USD / 2020 / 3500cc / diesel    → 28,400,000 ₸
Japan  / 5M KRW  / 2018 / 1500cc / hybrid   → 4,797,500 ₸
UAE    / 25k USD / 2022 / 2000cc / electric  → 17,917,500 ₸
Korea  / 20M KRW / 2015 / 1600cc / petrol   → 12,060,000 ₸
```
Run with: `npm run calc:sanity` (added to package.json)

---

## Immediate next actions (in priority order)

### 1. Run Supabase schema migration ⚠️ BLOCKS production lead form
Go to Supabase dashboard → SQL editor → run:
```sql
alter table public.leads
  alter column car_id drop not null,
  alter column importer_id drop not null;
alter table public.leads
  add column if not exists metadata jsonb,
  add column if not exists source text not null default 'catalog';
```

### 2. Replace WhatsApp placeholder
Search for `77071234567` in:
- `src/components/StickyCta.tsx`
- `src/components/RequestScreen.tsx`
- `src/components/CalculatorScreen.tsx`
Replace with real number.

### 3. Add desktop top-nav
Bottom nav (`display: none` at ≥ 720px). Tabs are unreachable from desktop.
Add a top nav bar for desktop showing all 4 tabs. This is a new component, styled to match existing `.topbar` / emerald accent system.

### 4. Deploy to Vercel + test on iPhone
After 1–3 done.

---

## Key constraints for next agent
- Do NOT disable Supabase RLS
- Do NOT expose admin data publicly  
- Do NOT add auth, payments, subscriptions (those are v0.3–v0.6)
- All user-facing UI must remain Russian
- Do not rewrite existing working components — add/extend only
- Pricing engine behavior must not change
- `npm run lint` and `npm run build` must remain clean after any changes

---

## Pricing engine facts (v2026.05, active rule)
- USD_TO_KZT=525, KRW_TO_KZT=0.39
- Delivery: Korea=980k, Japan=1.1M, UAE=1.35M, USA=1.45M KZT
- Utilization: petrol/diesel=430k, hybrid=360k, electric=180k KZT
- Fixed: broker=180k, registration=120k, importerDefault=600k KZT
- Customs: age ≤3y→newTo3Years, 4-7y→fourTo7Years, >7y→above7Years × vol ≤2000cc/above2000cc
- Rates: new/≤2000=12%, new/>2000=15%, 4-7y/≤2000=18%, 4-7y/>2000=22%, >7y/≤2000=25%, >7y/>2000=30%
