# FlowOps Agency Website

## Related
- [[agent-memory]]
- [[FlowOps promo video scene system]]

## Current status
- 2026-05-20 reapply: User clarified they confused the chat and asked to restore the just-reverted work. Reverted the revert commit `399df1a`, creating `ce2b162` (`Reapply "Launch FlowOps OS audit platform"`), and pushed `origin/main` so GitHub again contains the FlowOps OS/audit platform state. Local worktree is clean. The earlier Unicorn Studio local changes remain preserved in `stash@{0}`.
- 2026-05-20 revert: User disliked the just-pushed state and asked to remove it. Safely reverted commit `654fc70` with new commit `399df1a` (`Revert "Launch FlowOps OS audit platform"`) and pushed `origin/main`, returning GitHub to the pre-push content without force-pushing. Before reverting, preserved unrelated local uncommitted Unicorn Studio work in `stash@{0}`.
- 2026-05-20 Unicorn Studio integration: Embedded Unicorn Studio project `L2KwTdcziciksQX1Xd1C` into `/Users/tamerlan/Desktop/FlowOps2` as a dedicated post-hero WebGL band (`#unicorn-scene`). Added `unicornstudio-react`, created `components/sections/UnicornStudioSection.tsx` + CSS module, and inserted it after `HeroSection` in `app/page.tsx`. The scene uses SDK URL `https://cdn.jsdelivr.net/gh/hiunicornstudio/unicornstudio.js@v2.1.12/dist/unicornStudio.umd.js`, explicit responsive dimensions, lazy loading, production mode, a lightweight placeholder, and reduced render settings (`scale=0.85`, `dpi=1.25`, `fps=30`) to respect the recent performance concerns.
- 2026-05-20 Git sync: Committed and pushed the current `/Users/tamerlan/Desktop/FlowOps2` worktree to `origin/main`. Commit `654fc70` (`Launch FlowOps OS audit platform`) includes the FlowOps OS website, audit lead capture/internal pipeline/admin draft system docs/schema, and the performance patch. Local worktree is clean after push.
- 2026-05-20 performance pass: Investigated reported FlowOps2 lag and found likely global/runtime culprits: animated full-page `body::after` grain overlay with huge fixed inset, custom cursor doing continuous `requestAnimationFrame` plus layout reads/state updates on mousemove, Lenis smooth scroll running on touch/reduced-motion contexts, and hero video `currentTime` seeking on every ScrollTrigger update. Patched these without redesigning the public site: global grain is now static/subtle, custom cursor skips touch/reduced-motion and avoids per-frame layout reads, Lenis is disabled on touch/mobile/reduced-motion and shortened on desktop, and hero video seeking is throttled through one animation frame with a small change threshold.
- 2026-05-20 Stage 8A.1: Made Resend fully optional for MVP activation. Updated `scripts/check-env.mjs` so required MVP env vars are only `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, and `FLOWOPS_INTERNAL_ACCESS_KEY`; Resend, notification email, n8n webhook, and anon key are optional. Updated `.env.example`, `docs/local-secret-setup.md`, `docs/production-activation.md`, `docs/production-readiness-checklist.md`, and `docs/e2e-qa-checklist.md` to clarify that MVP launch works without Resend and that the preferred free notification path is `N8N_AUDIT_WEBHOOK_URL -> n8n -> Telegram`.
- 2026-05-19 Stage 8A: Added Production Activation Support & Live E2E Verification prep for `/Users/tamerlan/Desktop/FlowOps2`. Created `docs/local-secret-setup.md`, added safe `scripts/check-env.mjs` plus `npm run check:env`, added server-only `lib/config/env.ts` and wired `lib/supabase/server.ts` to use centralized Supabase env validation. Updated E2E/production docs with exact curl commands, local secret setup, env check, and `.env.local` git-ignore verification. No real secrets were used or printed.
- 2026-05-19 Stage 7: Completed Production Hardening & End-to-End QA Readiness for `/Users/tamerlan/Desktop/FlowOps2`. Added dependency-free in-memory rate limiting in `lib/security/rate-limit.ts`, applied it to public audit request submissions and internal PATCH routes, strengthened `lib/internal/access.ts` with SHA-256 constant-time comparison and production short-key fail-closed behavior, added ordered idempotent Supabase migrations under `supabase/migrations/`, and created `docs/e2e-qa-checklist.md`. Updated production activation/readiness docs for migration order, rate limiting, internal key warnings, serverless limitations, E2E QA, and npm audit findings.
- 2026-05-19 Stage 6: Added Internal Audit Response Drafting System V1 for `/Users/tamerlan/Desktop/FlowOps2`. Extended `supabase/schema.sql` with internal-only `audit_response_drafts`, added deterministic template generation in `lib/audit/create-response-draft.ts`, extended `lib/audit/admin.ts` with work item lookup/get-or-create draft/update draft helpers, added `/internal/audits/[id]?key=...`, `PATCH /api/internal/audit-response-drafts`, and `components/internal/AuditResponseDraftEditor.tsx`. `/internal/audits` now includes `Open draft` links. Added `docs/stage-6-audit-response-drafting.md` and updated production activation/readiness docs.
- 2026-05-19 Stage 5: Added Internal Admin Audit Workspace V1 for `/Users/tamerlan/Desktop/FlowOps2`. Added temporary shared-key access (`FLOWOPS_INTERNAL_ACCESS_KEY`), server-only access helper, internal audit admin data helpers, dynamic internal page at `/internal/audits?key=...`, PATCH API at `/api/internal/audit-work-items`, and a small client action component for updating work item status/follow-up/notes/next action. Public website navigation was not changed and no client portal/auth/payment was added. Added `docs/stage-5-internal-admin-workspace.md` and updated production activation/readiness docs.
- 2026-05-19 Stage 4: Added the Internal Audit Operations Pipeline foundation for `/Users/tamerlan/Desktop/FlowOps2`. Extended `supabase/schema.sql` with internal-only `audit_work_items`, added deterministic server-side work item creation in `lib/audit/create-work-item.ts`, wired `POST /api/audit-request` to create a work item after storing `audit_requests`, and enriched optional Resend/n8n payloads with work item IDs, detected pains, recommended systems, priority, and next action. Added `docs/stage-4-internal-audit-pipeline.md` and updated production activation/readiness docs for the two-table schema.
- 2026-05-19 Stage 3.5: Completed production activation audit for FlowOps audit request infrastructure. Inspected API route, Supabase server utility, Resend notification, n8n forwarder, schema, env example, Stage 3 docs, and frontend form. Added `docs/production-activation.md` with exact Supabase setup, env vars, local/production tests, and troubleshooting. Added `docs/production-readiness-checklist.md` covering build/lint, schema/RLS, env vars, end-to-end tests, secrets, rate limiting, and dependency review.
- 2026-05-19 Stage 3: Implemented real lead capture infrastructure for the Free AI Operations Audit request in `/Users/tamerlan/Desktop/FlowOps2`. Added `@supabase/supabase-js`, server-only Supabase service client, `POST /api/audit-request`, Supabase schema docs for `audit_requests`, Resend notification utility, optional n8n webhook forwarder, `.env.example`, and `docs/stage-3-lead-capture.md`. Rewired the premium audit form to POST to the API, added loading/error/success states, and added a hidden honeypot. API stores first in Supabase, then attempts Resend/n8n notifications without failing the request if notifications fail after storage.
- 2026-05-19 Stage 2: Added product depth and conversion logic to the FlowOps OS homepage without adding backend/auth/database/payments. Strengthened the AI Operations Audit as the main entry point with a five-step audit pipeline, diagnostic signals, illustrative priority/ROI cockpit, and example audit output panel. Deepened Systems Marketplace with compact product-module details for problem solved, workflows, recommended-fit, and deployment style. Upgraded Client Workspace Preview with monthly ops report, system status, and next improvements. Reworked final audit form into a frontend-only prepared request flow with explicit future Supabase/Resend/n8n TODO; it no longer posts to `/api/contact` or claims a live submission.
- 2026-05-19: Repositioned `/Users/tamerlan/Desktop/FlowOps2` homepage from a premium AI automation agency landing page into the first product-like **FlowOps OS / AI Operations Platform** version. Kept the dark cinematic visual language, GSAP/Lenis foundation, hero video treatment, and modular section structure. New homepage flow: Hero → Problem → FlowOps OS → Systems Marketplace → AI Operations Audit → Subscriptions → Client Portal Preview → Cases/System Patterns → FAQ → Free Audit CTA. No backend/auth/database/payment was added.
- 2026-05-05: Built the new cinematic FlowOps hero section in `/Users/tamerlan/Desktop/FlowOps2`: full-screen space video background from `public/hero-video.mp4`, GSAP/ScrollTrigger pinning, scroll-scrubbed video currentTime, staged hero text transforms, floating proof badges, exit fade, CSS star-field fallback, and mobile non-pinned looping video behavior. Homepage now renders `HeroSection` plus an empty black `#systems` section as the next-section transition target.
- 2026-05-05: In `/Users/tamerlan/Desktop/FlowOps2`, set up the new website foundation only: dark Space/Electric Blue/Amber CSS token system, Inter via `next/font/google`, Lenis provider, GSAP/ScrollTrigger helpers, custom cursor, and root layout wrappers. No page content or API routes were intentionally changed.
- 2026-04-26: Published the `FlowOps2` rebrand to GitHub production branch `main` for `https://github.com/TamerlanTA/FlowOps`. Commit: `d0c9d99` (`Launch FlowOps command-center rebrand`).
- Vercel project `flowops` built the new `main` commit successfully. Deployment `dpl_2ZrnYgHabX4VRyvMVrDUZW76UhmN` reached `READY` and aliases include `www.flowops.agency`, `flowops-orpin.vercel.app`, and `flowops-git-main-tamertt931-8560s-projects.vercel.app`.
- Deployment was done from a clean temporary worktree at `/tmp/flowops-main-worktree` because `/Users/tamerlan/Desktop/PC/FlowOps` remained on dirty branch `milestone4-auth-completion`.
- 2026-04-26: Created a new full-rebrand implementation in `/Users/tamerlan/Desktop/FlowOps2` rather than continuing to mutate `/Users/tamerlan/Desktop/PC/FlowOps`. The new site keeps FlowOps content/offer only, but replaces the visual system with a new dark command-center portfolio aesthetic inspired by the user's two GitHub references.
- New `FlowOps2` build uses a single custom client experience (`components/FlowOpsRebrand.tsx`) with fixed glass nav, full-screen canvas engine/orbit scene, interactive workflow prompt, live architecture preview, stack strip, problems, services, cases, method, proof, and contact sections.
- Preserved the old lead capture contract by copying `/api/contact`, `/api/sendLead`, `lib/lead.ts`, and Telegram/Google Sheets/SendGrid integration helpers into `FlowOps2`; the UI posts to `/api/contact` with the same schema.
- 2026-04-26: Upgraded `/Users/tamerlan/Desktop/PC/FlowOps` homepage from a standard agency landing page into an interactive premium AI Automation System Builder experience.
- Hero now uses a live builder: user enters an automation request, UI immediately shows a fallback/generated system map, then calls `/api/analyze-system` for AI-backed structured architecture when `OPENAI_API_KEY` is available.
- Added deterministic fallback analyzer for common automation keywords: WhatsApp, email, CRM, leads, Shopify, orders, Google Sheets, Airtable, Notion, ClickUp, Slack, invoices, scraping, reporting, dashboard, support, and customer messages.
- Preserved existing lead form submission pipeline (`/api/sendLead` → Telegram, Google Sheets, email) and added prefill from generated system request/summary.
- Added six interactive Real Systems cards: WhatsApp AI Bot, WhatsApp AI Bot + CRM Sync, Lead Research Automation Pipeline, Social Media Video Automation Pipeline, Jotform to ClickUp Operations System, AI Label Generator Order Flow.
- Added How We Think section and updated final CTA/SEO positioning around systems, not random automations.
- 2026-04-26: Restyled the new builder toward the user's monochrome reference screenshots (`IMG_7326.jpg`, `IMG_7327.jpg`, `IMG_7328.jpg`): black framed stage, compact centered pill nav, white CTA, grayscale mesh/silk background, softer horizon arc, reduced cyan/violet SaaS look, and monochrome workflow/result cards.
- 2026-04-26: Added a second polish pass focused on interaction quality: staged system-generation states, living idle graph, edge labels/particles, node type badges/hover states, mobile compact flow preview, executive-style result panel, case modal tools + “Build something similar” prefill CTA, monochrome Real Systems cards, updated contact copy, and updated metadata title/description.
- 2026-04-26: Added a GitHub-reference effects pass inspired by `mohitvirli.github.io` and `ThreeJS_Portfolio`: lightweight canvas automation-engine orbit layer, pointer-responsive hero stage tilt/spotlight, portal/depth hover treatment for workflow nodes and case cards, scan highlights for glass panels, and command-bar polish. No new heavy animation/3D dependencies were added.
- 2026-04-26: Addressed reported hydration mismatch warning by adding root `suppressHydrationWarning` on `html`/`body` for extension-injected attributes and fixing Navbar hash-route active comparison (`/#cases`) to compare only the pathname.

## Key decisions
- 2026-05-20 Unicorn Studio integration: Place the WebGL scene in a separate post-hero section instead of replacing the existing hero video/ScrollTrigger system. This preserves current conversion copy and avoids stacking another heavy WebGL layer directly on the pinned hero runtime. Keep the vendor watermark visible unless the Unicorn project/account supports removing it legitimately.
- 2026-05-20 performance pass: Preserve the cinematic dark direction but prefer static or scoped visual texture over global full-viewport infinite overlays. Keep Lenis and custom cursor as desktop-only enhancement layers, not mandatory runtime behavior for touch/mobile or reduced-motion users.
- 2026-05-20 Stage 8A.1: Do not use Resend for MVP activation to avoid paid email-provider spend. Minimum activation is Supabase storage plus internal workspace monitoring. Keep Resend code as optional future integration; use optional n8n->Telegram for free notifications if needed.
- 2026-05-19 Stage 8A: Keep activation diagnostics local and non-secret-bearing. Added a local env presence script instead of a public diagnostic endpoint. The script reports only present/missing and exits nonzero when minimum lead-capture variables are absent.
- 2026-05-19 Stage 7: Use lightweight in-memory rate limiting as a temporary V1 hardening layer only: public audit requests are limited to 5 per 10 minutes per IP, internal PATCH routes to 60 per 10 minutes per IP/internal-key fingerprint. This is not sufficient for multi-instance/serverless production at scale and should later be replaced by Upstash Redis, Vercel KV, or Supabase-backed rate limiting.
- 2026-05-19 Stage 6: Response drafts are deterministic/template-based only. No LLM/API calls, no automatic client email sending, and no claim of completed or AI-generated audit reports. Drafts are internal preparation artifacts that require human review before any manual client follow-up.
- 2026-05-19 Stage 5: Use a temporary internal shared-key access model instead of adding a full auth provider. The configured key lives only in `FLOWOPS_INTERNAL_ACCESS_KEY`; the admin page accepts `?key=...`, and the update API requires `x-flowops-internal-key`. This is explicitly temporary and should be replaced with proper authenticated admin access before broader team use.
- 2026-05-19 Stage 4: Use deterministic keyword/rule-based internal classification only. No LLM calls, no fake generated audit report, and no public/internal dashboard were added. `audit_work_items` is server-created and internal-only with RLS enabled and no public policies; future admin access must go through authenticated service/admin infrastructure.
- 2026-05-19 Stage 3: Use a server-route capture model for audit requests. Browser submits to Next.js API; API uses `SUPABASE_SERVICE_ROLE_KEY` server-side only. `audit_requests` has RLS enabled and no public SELECT/UPDATE/DELETE policies. No anon insert policy is required for the preferred server route model.
- 2026-05-19 Stage 2: Keep the final audit request frontend-only and honest until a real submission path is approved. Success copy says “Audit Request Prepared,” not sent/submitted. The form includes a code TODO for future Supabase, Resend, or n8n integration.
- 2026-05-19 FlowOps OS pass: Treat FlowOps as a productized AI operations platform, not a generic agency. Use the free AI Operations Audit as the primary conversion event. Productize packaged systems as LeadOS, SalesOS, VoiceOS, InboxOS, OpsOS, and ReportOS. Productize recurring work into Maintain, Scale, and Operator subscription modes. Cases are now framed as system patterns and example operational effects rather than unsupported hard client metrics.
- 2026-05-05 hero pass: Used Lucide SVG icons for stat badges instead of emoji glyphs to keep icon rendering consistent with the UI/UX quality checklist. Used `/hero-video.mp4` as the served asset path even though the source file lives at `public/hero-video.mp4`, because Next serves `public` files from the site root.
- 2026-05-05: Treat the current rebuild as a foundation pass. Page sections/content should be rebuilt later against the new design system rather than expanded during this infrastructure step.
- User explicitly asked for a full rebrand and to reuse only the old site's information; therefore the old visual components were not reused in `FlowOps2`.
- GitHub clone/download of the two reference repos stalled on network, so the `FlowOps2` implementation adapted the referenced effect direction from the known pattern: canvas orbit/data particles, horizon grid, scan-panel hover, portal-like cards, and command-panel workflow UI.
- `FlowOps2` remains a compact single-page site for now, with old multi-page routes intentionally not recreated unless requested.
- Treat this as a small project note for now, not a full project folder, unless the agency website becomes a larger ongoing redesign/build stream.
- Avoided React Flow or large new dependencies; implemented a custom HTML/SVG-style workflow renderer with existing React, Tailwind, and lucide-react.
- API route always returns a valid `SystemPlan`; OpenAI failure or missing key falls back silently to deterministic local planning.
- Used OpenAI Responses API with structured JSON schema via direct `fetch`, avoiding a new SDK dependency.
- For the reference-style pass, kept the interactive builder and lead prefill behavior intact instead of replacing the page with a static portfolio-like hero.
- Continued avoiding heavy animation libraries; polish uses existing React state plus CSS transitions/keyframes for performance.
- For the GitHub-reference pass, adapted visual techniques instead of copying the repos directly: 2D canvas approximates orbit/ring/data movement, CSS handles portal/depth hover, and pointer CSS variables provide a “camera” feel without Three.js/GSAP runtime cost.

## Validation
- 2026-05-20 reapply: `git revert --no-edit 399df1a` succeeded, `git push origin main` succeeded (`399df1a..ce2b162 main -> main`), and `git status --short` returned clean.
- 2026-05-20 revert: `git revert --no-edit 654fc70` succeeded, `git push origin main` succeeded (`654fc70..399df1a main -> main`), and `git status --short` returned clean. Preserved pre-revert local uncommitted work in `stash@{0}`.
- 2026-05-20 Unicorn Studio integration: `npm run lint` passed. `npm run build` passed; build emitted the existing Node `DEP0205 module.register()` deprecation warnings from tooling. Production server ran on `http://127.0.0.1:3010` for QA. Playwright Chromium checks verified `#unicorn-scene canvas` renders on desktop (`1440x1100`, frame `1180x560`) and mobile (`390x844`, frame `358x335`, no horizontal overflow). Screenshots saved temporarily at `/tmp/flowops-unicorn-desktop.png` and `/tmp/flowops-unicorn-mobile.png`.
- 2026-05-20 Git sync: `git push origin main` succeeded (`b041bc4..654fc70 main -> main`). `git status --short` returned clean after push.
- 2026-05-20 performance pass: `npm run lint` passed with zero warnings. `npm run build` passed; build emitted Node `DEP0205 module.register()` deprecation warnings from tooling but completed successfully. Existing dev server was already listening on `http://localhost:3000` and returned HTTP 200; a second `next dev` attempt correctly failed on the existing `.next/dev/lock`, so the running process was not terminated.
- 2026-05-20 Stage 8A.1: `npm run check:env` passed with `.env.local` present and printed no secret values; required MVP vars were present and optional Resend/n8n vars were missing without failing. `npm run lint` passed with zero warnings. `npm run build` passed with `.env.local` loaded. Code inspection confirmed missing Resend env returns skipped, missing n8n webhook returns skipped, and audit request notifications are isolated behind `Promise.allSettled` after Supabase insert.
- 2026-05-19 Stage 8A: `git check-ignore -v .env.local` confirmed `.env.local` is ignored by `.gitignore`. `npm run check:env` ran safely and printed no secret values; it failed as expected because `.env.local` was not present and required Supabase env vars were missing. `npm run lint` passed with zero warnings. `npm run build` passed. Live E2E remains pending until real Supabase/Resend/n8n/internal key values are configured by the user.
- 2026-05-19 Stage 7: `npm run lint` passed with zero warnings. `npm run build` passed and preserved dynamic routes `/api/audit-request`, `/api/internal/audit-work-items`, `/api/internal/audit-response-drafts`, `/internal/audits`, and `/internal/audits/[id]`. `npm audit --audit-level=moderate` reported 7 vulnerabilities (3 moderate, 4 high); no fix was run because `npm audit fix --force` would move Next.js outside the current dependency range.
- 2026-05-19 Stage 6: `npm run lint` passed with zero warnings. `npm run build` passed and generated dynamic routes `/internal/audits/[id]` and `/api/internal/audit-response-drafts`. Security grep confirmed `audit_response_drafts` is server/internal only, RLS is enabled in schema, and no new email-sending path was added for drafts. No live Supabase draft creation was tested because real secrets/project access were not available.
- 2026-05-19 Stage 5: `npm run lint` passed with zero warnings. `npm run build` passed and generated dynamic routes `/internal/audits` and `/api/internal/audit-work-items`. Security grep confirmed `SUPABASE_SERVICE_ROLE_KEY` remains server-side/docs/env placeholder only; `FLOWOPS_INTERNAL_ACCESS_KEY` is read in `lib/internal/access.ts` and documented as a placeholder. The client update component only receives the provided URL key for the temporary V1 PATCH flow.
- 2026-05-19 Stage 4: `npm run lint` passed with zero warnings. `npm run build` passed and generated `/api/audit-request`. Security grep confirmed `SUPABASE_SERVICE_ROLE_KEY` is still only in server utility/docs/env placeholder and not in client components. `supabase/schema.sql` enables RLS on both `audit_requests` and `audit_work_items` and creates no public policies.
- 2026-05-19 Stage 3.5: Security model confirmed by code inspection and `rg`: `SUPABASE_SERVICE_ROLE_KEY` appears only in server utility/API/docs/env placeholder, not client components; browser posts to `/api/audit-request`; API validates before insert; `supabase/schema.sql` enables RLS and creates no public policies; notification failures happen after insert and are isolated. `npm run lint` passed with zero warnings. `npm run build` passed and generated `/api/audit-request`.
- 2026-05-19 Stage 3: `npm run lint` passed with zero warnings. `npm run build` passed and generated `/api/audit-request`. Local dev server ran at `http://localhost:3001` because port 3000 was occupied. Curl smoke test with valid audit payload and no Supabase env returned expected HTTP 503 configuration error in development. Curl honeypot payload returned expected HTTP 400 safe rejection. No real secrets were used.
- 2026-05-19 Stage 2: `npm run build` passed. `npm run lint` passed with zero warnings after cleaning legacy unused imports. Dev server ran at `http://localhost:3001` because port 3000 was occupied. Local HTML smoke check confirmed Stage 2 copy rendered: `Map current workflows`, `Client Workspace Preview`, `Monthly ops report`, `Problem solved`, and `No tool replacement required`.
- 2026-05-19 FlowOps OS pass: `npm run build` passed. `npm run lint` passed with warnings only: unused import warnings in no-longer-rendered `BeforeAfterSection.tsx` / `TestimonialsSection.tsx`, plus an existing `ROICalculator.tsx` hook dependency warning. Dev server ran at `http://localhost:3001` because port 3000 was occupied. Local HTML smoke check confirmed rendered copy includes `FlowOps OS`, `AI Operations Platform`, `LeadOS`, `Client Portal Preview`, and `Request Free Audit`.
- 2026-05-05 hero pass: `npm run build` passed. Playwright CLI/browser checks on `http://localhost:3000` confirmed desktop has one pin spacer, scroll advances video currentTime from ~0s to full duration, staged text/badges/exit overlay opacity changes during scroll, and console had 0 errors/warnings. Mobile `390x844` check confirmed no pin spacer, video `autoplay=true`, `loop=true`, not paused, and badges hidden. `npm run lint` still fails only on protected Session A file `components/providers/LenisProvider.tsx` (`react-hooks/set-state-in-effect`); the new hero files no longer add lint errors.
- 2026-05-05 foundation pass: `npm run build` passed. Dev server ran at `http://localhost:3000`. Chromium CDP smoke check confirmed `body` background `rgb(0, 0, 0)`, custom cursor dot/ring visible after mouse movement, `html` had the Lenis class, and no browser console warnings/errors were emitted during load. `npx playwright screenshot` captured `/tmp/flowops-foundation.png`.
- Production deploy validation: Vercel deployment for commit `d0c9d995b4565718bc4ae0277c3d9b0c609de0e0` is `READY`; Vercel fetch of `https://flowops-orpin.vercel.app` returned HTTP 200 and contained the new command-center rebrand HTML.
- Clean `main` worktree validation before push: `npm run lint` passed and `npm run build` passed.
- `FlowOps2`: `npm run lint` passed cleanly.
- `FlowOps2`: `npm run build` passed cleanly; routes generated: `/`, `/api/contact`, `/api/sendLead`, `/robots.txt`, `/sitemap.xml`.
- `FlowOps2`: Dev server ran at `http://localhost:3002`.
- `FlowOps2`: Chrome headless screenshots checked desktop `1440x1100` and mobile `390x844`; desktop hero/canvas/command panel rendered correctly. Mobile hero initially overflowed, then was adjusted with mobile type/action/layout constraints and `overflow-x: hidden`.
- `FlowOps2`: `/api/contact` smoke test with invalid payload returned expected schema field errors.
- `npm run lint` passed with one existing warning in `components/Services.tsx` about a missing hook dependency.
- `npm run build` passed.
- Dev server ran at `http://localhost:3001` because port 3000 was already in use.
- Playwright screenshot fallback verified desktop and mobile hero rendering plus desktop Real Systems section.
- API smoke check against `/api/analyze-system` returned valid fallback JSON for a Shopify → ClickUp → Slack reporting request.
- Reference-style pass validation: `npm run lint` passed with the same pre-existing `components/Services.tsx` warning, `npm run build` passed, Playwright Chrome screenshots checked desktop `1440x1100` and mobile `390x844`, and `/api/analyze-system` returned valid fallback JSON for a WhatsApp → CRM → Slack reporting request.
- Interaction polish validation: `npm run lint` passed with the same pre-existing `components/Services.tsx` warning, `npm run build` passed, Playwright Chrome screenshots checked desktop `1440x1100` and mobile `390x844`, and `/api/analyze-system` returned valid fallback JSON for WhatsApp → Kommo CRM → Slack.
- GitHub-reference effects validation: `npm run lint` passed with the same pre-existing `components/Services.tsx` warning, `npm run build` passed, Playwright Chrome screenshots checked desktop `1440x1100`, mobile `390x844`, and desktop Real Systems section. `/api/analyze-system` returned valid JSON for “Qualify WhatsApp leads and update Kommo CRM”.
- Hydration warning fix validation: `npm run lint` passed with the same pre-existing `components/Services.tsx` warning and `npm run build` passed after the Navbar nullable pathname type fix.

## Residual risks
- 2026-05-20 Unicorn Studio integration: The rendered scene includes a visible `Made with unicorn.studio` watermark from the project/runtime. Browser QA also surfaced existing GSAP target warnings from other sections plus WebGL performance `ReadPixels` warnings during headless capture; no new fatal errors were observed. Re-check real-device performance before production deploy because this adds a second rich visual runtime to the homepage.
- 2026-05-20 performance pass: Browser/performance profiling in DevTools was not completed in this pass. If lag remains, next targets are section-level `backdrop-filter` and large blur layers, the 2.5s preloader, and further simplifying the hero ScrollTrigger pin/video scrub on lower-powered devices.
- 2026-05-20 Stage 8A.1: Live E2E still needs to be completed against actual Supabase with the MVP env vars. Without Resend or n8n, new leads must be monitored manually via `/internal/audits?key=...`.
- 2026-05-19 Stage 8A: Live production Supabase/Resend/n8n verification has not been performed because no real `.env.local` or production secrets were available in the workspace. The next activation step requires user-provided secrets applied locally/hosting-side without committing them.
- 2026-05-19 Stage 7: Rate limiting is in-memory and resets on server restart; it does not synchronize across serverless instances. Dependency audit still has unresolved findings, including high-severity Next.js advisories that require a version/range decision rather than automatic fix. No live production E2E run was performed because real Supabase/env secrets were not available.
- 2026-05-19 Stage 6: `audit_response_drafts` requires applying the updated schema in production. The draft editor still inherits the Stage 5 shared-key model, so anyone with the key can open/edit drafts. Draft status `sent` is a manual marker only; the app does not send client emails.
- 2026-05-19 Stage 5: The internal workspace uses a shared URL/header key, not real authentication. Anyone with the key can view/update internal audit work items, and the key is passed to the client component for PATCH updates in V1. Treat it like a password, keep the route unlinked, rotate if exposed, and replace with proper authenticated admin access before broader use.
- 2026-05-19 Stage 4: No live Supabase insert was run because real secrets/project access were not available. Production must re-apply `supabase/schema.sql` so `audit_work_items` exists before expecting internal work item creation. If missing, lead capture still succeeds but internal work item creation logs an error and must be manually triaged.
- 2026-05-19 Stage 3.5: No production secrets or live Supabase project were available, so real production insert/email/n8n verification still must be performed after env vars are configured and schema is applied.
- 2026-05-19 Stage 3: Production capture still requires applying `supabase/schema.sql` and setting env vars (`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, optional Resend/n8n values). No live Supabase insert test was run because no real secrets were used locally.
- 2026-05-19 Stage 3: `npm install @supabase/supabase-js` reported existing dependency audit findings: 7 vulnerabilities (3 moderate, 4 high). No audit fix was run to avoid unrelated dependency churn.
- 2026-05-19 Stage 3: Anti-spam is basic honeypot + field validation only. Add rate limiting before paid traffic or high-volume launch.
- 2026-05-19 Stage 2: Full browser screenshot QA still not completed because Playwright is not installed locally. Manual desktop/mobile visual review remains recommended before deploy.
- 2026-05-19 Stage 2: Final audit form is intentionally not wired to a backend; conversion will require a later Supabase/Resend/n8n implementation.
- 2026-05-19 FlowOps OS pass: No full browser screenshot QA was completed because local Playwright is not installed in the workspace. Manual visual review in a real browser is recommended before deployment, especially mobile nav density and long section rhythm.
- 2026-05-19 FlowOps OS pass: The new Client Portal section is a static product preview only; no backend, auth, database, or real portal visibility exists yet by design.
- 2026-05-19 FlowOps OS pass: Repo was already dirty with prior uncommitted/untracked website rebuild files; preserve unrelated changes when staging/committing.
- 2026-05-05 hero pass: The next section is currently an empty black `#systems` placeholder so the hero exit fade has somewhere to transition. Future section builds should replace it with real content without changing the hero foundation.
- 2026-05-05 hero pass: `npm run lint` is blocked by `components/providers/LenisProvider.tsx`, which was explicitly protected in the hero request and not modified.
- 2026-05-05: `npm install` reported 7 audit vulnerabilities (3 moderate, 4 high) in the dependency tree. No audit fix was run because that could change package versions beyond the requested install.
- 2026-05-05: Replacing `globals.css` with the new foundation removes the prior page-specific global styling. This matches the infrastructure-only rebuild request, but existing page content will need a future rebuild/restyle pass to look production-ready again.
- `/Users/tamerlan/Desktop/PC/FlowOps` is still the old local dirty `milestone4-auth-completion` worktree; production `main` was updated through `/tmp/flowops-main-worktree`. If future local edits should start from production, switch/clone from `main` carefully instead of continuing from the dirty milestone branch.
- `FlowOps2`: Mobile Chrome headless screenshot did not visibly show the hamburger button even after CSS adjustments; main content/CTA rendered, but manual browser check is recommended before deployment.
- `FlowOps2`: Valid contact submission was not smoke-tested because production env variables for Telegram, Google Sheets, and SendGrid may be absent locally; invalid-payload validation path was verified.
- `FlowOps2`: Reference repos were not fully cloned due network stalls, so no direct code/assets were copied from them.
- Computer Use browser interaction could not complete because macOS Accessibility/Screen Recording permissions were still pending.
- Modal click-through was not fully browser-automated due missing local `@playwright/test`; visual/API checks covered the main risks.
- Existing lint warning in `components/Services.tsx` remains unrelated to this work.
- `test-results/.last-run.json` remains untracked from a failed temporary Playwright test attempt; deletion needs explicit user confirmation.

## Next steps
- If deploying the Unicorn scene, manually review the live page on desktop and mobile after Vercel deploy, especially the transition from the pinned hero into `#unicorn-scene` and the visible Unicorn watermark.
- If the site still feels heavy after the performance patch, profile `http://localhost:3000` in Chrome Performance and test a second pass that reduces section-level `backdrop-filter`, large blurred glows, and the cinematic preloader duration.
- Run the MVP E2E path without Resend: submit public audit request, verify `audit_requests`, `audit_work_items`, internal audits page, response draft creation, and status/draft editing.
- If free notifications are desired, configure `N8N_AUDIT_WEBHOOK_URL` to an n8n workflow that sends Telegram messages.
- Create `.env.local` using `docs/local-secret-setup.md`, run `npm run check:env`, apply Supabase migrations, then run `docs/e2e-qa-checklist.md` against local or staging.
- Configure production host env vars without exposing secrets, deploy, and repeat the E2E checklist against the production URL.
- Run `docs/e2e-qa-checklist.md` against the configured production/staging environment after applying migrations and env vars.
- Decide the dependency upgrade path for the npm audit findings, especially Next.js advisories, without relying on `npm audit fix --force` blindly.
- Replace in-memory rate limiting with shared persistent rate limiting before paid traffic or multi-instance production use.
- Apply the updated Stage 6 `supabase/schema.sql` in production, open a real work item draft from `/internal/audits`, verify an `audit_response_drafts` row is created, and test editing/saving the draft.
- Before adding AI-assisted draft generation or email sending, add authenticated admin access, audit logs, review gates, and explicit send controls.
- Configure `FLOWOPS_INTERNAL_ACCESS_KEY` in production only if the internal audit workspace will be used, then test `/internal/audits?key=...` and a PATCH status update against a real `audit_work_items` row.
- Replace Stage 5 shared-key access with real authenticated admin access before adding more internal users, richer notes, assignment, or audit logs.
- Apply the updated Stage 4 `supabase/schema.sql` in production, submit a real audit request, and verify both `audit_requests` and `audit_work_items` rows are created with expected triage fields.
- Before building any internal workspace, define authenticated admin/service access for `audit_work_items`; do not expose the table directly to the browser.
- Follow `docs/production-activation.md` and `docs/production-readiness-checklist.md` before deployment: apply schema, configure env vars, deploy, submit a real frontend request, verify Supabase row, verify Resend, and verify n8n if configured.
- Apply `supabase/schema.sql` in the target Supabase project, add production env vars, and submit a real audit request through the website to verify Supabase insert + Resend + optional n8n path.
- Decide whether to keep both legacy `/api/contact` lead capture and new `/api/audit-request`, or gradually migrate all website conversion paths to the Stage 3 audit request model.
- Decide the real submission path for the audit request form: Supabase table + Resend email, n8n webhook, or existing `/api/contact` integration revised for the new audit payload.
- Manual QA the Stage 2 page in a real browser on desktop/mobile before deploy; pay attention to audit pipeline density, final form step layout, systems marketplace panel height, and portal preview readability.
- Manually review `http://localhost:3001` in desktop and mobile viewport before deployment; focus on navigation density, hero copy fit, Systems Marketplace tab overflow, Portal Preview legibility, and final audit form flow.
- If approved, do a deployment pass from the dirty worktree carefully: stage only the intended FlowOps OS website files and preserve unrelated existing changes.
- Later product roadmap: replace the static Client Portal preview with real auth, client system health data, saved-hours reporting, lead-flow metrics, and monthly recommendations only after the marketing repositioning is approved.
- Build the first real post-hero section at `#systems` and connect the secondary CTA to meaningful live-systems content.
- Build the actual FlowOps page sections/components on top of the new dark foundation, using the new CSS variables, Lenis scroll, GSAP helpers, and cursor data attributes.
- Decide whether to address the npm audit findings now or after the page rebuild, to avoid unrelated dependency churn during design implementation.
- Manually open `https://www.flowops.agency` and `https://flowops-orpin.vercel.app` to check final visual behavior on real desktop/mobile browsers.
- Decide whether to clean up or archive the old dirty `milestone4-auth-completion` local worktree after confirming production is good.
- Open `http://localhost:3002` manually in Chrome/Safari and check mobile nav/hamburger behavior, hero CTA scroll, prompt editing, and contact form layout.
- If the new rebrand direction is approved, decide whether `FlowOps2` replaces `/Users/tamerlan/Desktop/PC/FlowOps` or should be deployed as a separate project.
- If closer fidelity to the GitHub references is needed, retry cloning/downloading the reference repos and consider adding a real Three.js layer only if performance remains acceptable.
- Manually click through hero input, Real Systems modal, and Get this system built CTA in the browser once Computer Use permissions or normal browser access is available.
- Set `OPENAI_API_KEY` in production if AI-generated architecture should be used instead of deterministic fallback.
- Consider a later polish pass for the dedicated `/cases` page so it visually matches the new Real Systems section.
- If continuing the reference direction, consider making downstream sections (`RealSystems`, contact, services) more monochrome so the whole page matches the new hero/nav system.
- Optional next polish: make older reused sections (`Problems`, `Solutions`, `Services`, `TechStack`, `RoiCalculator`, footer) fully match the monochrome command-center aesthetic; dedicated `/cases` still uses older styling.
- Optional next polish: if the user wants closer fidelity to the GitHub refs, add real `@react-three/fiber` scenes only for one isolated hero layer, after confirming performance budget and asset direction.
